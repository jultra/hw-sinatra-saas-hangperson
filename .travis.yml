language: ruby
rvm:
  - "2.2.2"
before_install:
  - apt-get update -qq
  - apt-get install -y libmagic-dev
bundler_args: ''
script:
  - travis_retry cucumber install/install.feature
  - cucumber features